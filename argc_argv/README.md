# C - argc, argv

This project teaches how to read and use command-line arguments in C via `argc` and `argv`. You’ll learn that `argv` is a list of strings (with `argv[0]` being the program name), how to loop through inputs, convert them to numbers when needed, validate user input, and return proper error codes for incorrect usage.