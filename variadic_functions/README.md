## C - Variadic functions

This project focuses on variadic functions in C. It uses `stdarg.h` and the macros `va_start`, `va_arg`, and `va_end` to handle a variable number of arguments in functions such as `sum_them_all`.