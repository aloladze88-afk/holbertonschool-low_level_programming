## Recursion

This project introduces recursion in C: a function solving a problem by calling itself on a smaller part of that same problem until it reaches a base case. In this chapter, the main goal is to understand what recursion is, how to write recursive functions correctly, and when recursion is useful or not useful.