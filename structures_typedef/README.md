# C - Structures, typedef

This project teaches the basics of structures and typedef in C. A structure is used to store several related values in one variable, while `typedef` gives a type a shorter and more convenient name. These tools help make programs more organised and easier to understand when working with grouped data.